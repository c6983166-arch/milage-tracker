name: Build Hidden Treasures Label Printer APK

on:
  workflow_dispatch:
  push:
    branches:
      - agent/hidden-treasures-label-printer-v1.3.2
    paths:
      - 'ht-label-printer-v1.2/**'
      - '.github/workflows/build-hidden-treasures-label-printer.yml'

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Java 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - name: Restore Hidden Treasures signing key
        uses: actions/cache@v4
        with:
          path: ~/.android/debug.keystore
          key: hidden-treasures-label-printer-signing-key-v1

      - name: Ensure permanent signing key exists
        run: |
          mkdir -p "$HOME/.android"
          if [ ! -f "$HOME/.android/debug.keystore" ]; then
            keytool -genkeypair -keystore "$HOME/.android/debug.keystore" -storepass android -alias androiddebugkey -keypass android -dname "CN=Hidden Treasures Android,O=Hidden Treasures,C=US" -keyalg RSA -keysize 2048 -validity 10000
          fi
          test -f "$HOME/.android/debug.keystore"

      - name: Set up Gradle 8.9
        uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '8.9'

      - name: Build debug APK
        working-directory: ht-label-printer-v1.2
        run: gradle --no-daemon assembleDebug

      - name: Verify package identity and install metadata
        run: |
          APK=ht-label-printer-v1.2/app/build/outputs/apk/debug/app-debug.apk
          AAPT=$(find "$ANDROID_HOME/build-tools" -name aapt -type f | sort -V | tail -1)
          "$AAPT" dump badging "$APK" | tee /tmp/badging.txt
          grep "package: name='com.hiddentreasures.htlabelprinter' versionCode='6' versionName='1.3.2'" /tmp/badging.txt
          grep "sdkVersion:'26'" /tmp/badging.txt

      - name: Verify APK alignment
        run: |
          APK=ht-label-printer-v1.2/app/build/outputs/apk/debug/app-debug.apk
          ZIPALIGN=$(find "$ANDROID_HOME/build-tools" -name zipalign -type f | sort -V | tail -1)
          "$ZIPALIGN" -c -P 16 -v 4 "$APK"

      - name: Verify APK signature
        run: |
          APKSIGNER=$(find "$ANDROID_HOME/build-tools" -name apksigner -type f | sort -V | tail -1)
          "$APKSIGNER" verify --verbose --print-certs ht-label-printer-v1.2/app/build/outputs/apk/debug/app-debug.apk

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: Hidden-Treasures-Label-Printer-v1.3.2-CLEAN-INSTALL-APK
          path: ht-label-printer-v1.2/app/build/outputs/apk/debug/app-debug.apk
          if-no-files-found: error
